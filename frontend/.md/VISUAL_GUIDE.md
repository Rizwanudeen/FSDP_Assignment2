# Cross-Agent Reply System - Visual Guide

## User Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                     CROSS-AGENT CONVERSATION FLOW                    │
└─────────────────────────────────────────────────────────────────────┘

STEP 1: User Gets Response from Bot A
┌──────────────┐
│   Bot A      │  "Why is the sky blue?"
│  Responds    │  → "It's Rayleigh scattering..."
└──────────────┘
       │
       │ User likes this response!
       ▼
    [💚 Like]  ──→  [Save for Cross-Replies]
       │
       │
       ▼
STEP 2: Create Cross-Reply Session (POST)
┌────────────────────────────────────────┐
│ POST /api/agents/cross-replies         │
│ {                                      │
│   originalMessageId: "msg-123",        │
│   originalAgentId: "bot-a",            │
│   originalConversationId: "conv-456",  │
│   title: "Why is sky blue?",           │
│   questionContent: "..."               │
│ }                                      │
└────────────────────────────────────────┘
       │
       │ Response: { id: "session-xyz" }
       ▼
STEP 3: User Chats with Bot B
┌──────────────┐
│   Bot B      │  Same Question: "Why is the sky blue?"
│  Responds    │  → "It's the scattering of light..."
└──────────────┘
       │
       │ Got response message ID: "msg-789"
       ▼
STEP 4: Add Bot B Response (POST)
┌──────────────────────────────────────────────┐
│ POST /api/agents/cross-replies/:id/responses │
│ {                                            │
│   agentId: "bot-b",                         │
│   conversationId: "conv-789",               │
│   responseMessageId: "msg-789"              │
│ }                                            │
└──────────────────────────────────────────────┘
       │
       ▼
STEP 5: User Chats with Bot C
┌──────────────┐
│   Bot C      │  Same Question: "Why is the sky blue?"
│  Responds    │  → "The blue sky is a natural wonder..."
└──────────────┘
       │
       │ Got response message ID: "msg-101"
       ▼
STEP 6: Add Bot C Response (POST)
┌──────────────────────────────────────────────┐
│ POST /api/agents/cross-replies/:id/responses │
│ {                                            │
│   agentId: "bot-c",                         │
│   conversationId: "conv-101",               │
│   responseMessageId: "msg-101"              │
│ }                                            │
└──────────────────────────────────────────────┘
       │
       ▼
STEP 7: View All Responses (GET)
┌──────────────────────────────────────┐
│ GET /api/agents/cross-replies/:id    │
└──────────────────────────────────────┘
       │
       ▼
   ┌────────────────────────────────────────────────┐
   │  ✨ COMPARISON VIEW ✨                         │
   ├────────────────────────────────────────────────┤
   │ Q: Why is the sky blue?                        │
   │                                                │
   │ 🤖 Bot A (Physics):                           │
   │ "It's Rayleigh scattering..."                │
   │                                                │
   │ 🤖 Bot B (Scientist):                         │
   │ "It's the scattering of light..."            │
   │                                                │
   │ 🤖 Bot C (Poet):                              │
   │ "The blue sky is a natural wonder..."         │
   │                                                │
   └────────────────────────────────────────────────┘
```

---

## Data Model Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE SCHEMA                           │
└─────────────────────────────────────────────────────────────┘

Users
┌──────────────────┐
│ id               │◄──────────────────┐
│ email            │                   │
│ password         │                   │
│ created_at       │                   │
└──────────────────┘                   │
                                       │
                                       │ user_id
                                       │
                    ┌──────────────────────────────────┐
                    │   CrossAgentReplies              │
                    │ ┌──────────────────────────────┐ │
                    │ │ id                           │ │
                    │ │ user_id                ├─────┤─┼─→ Users
                    │ │ original_message_id    ├────→ │ │
                    │ │ original_agent_id      ├────→ │ │
                    │ │ original_conversation_id ├──→ │ │
                    │ │ title                  │ │    │ │
                    │ │ question_content       │ │    │ │
                    │ │ created_at             │ │    │ │
                    │ └────┬────────────────────┘ │    │ │
                    └────────────────────────────────┘    │
                         │                               │
                    ┌────┴───────────────────────┐        │
                    │                            │        │
                    │ (One-to-Many)              │        │
                    ▼                            │        │
        ┌───────────────────────────────────┐   │        │
        │  CrossAgentResponses              │   │        │
        │ ┌─────────────────────────────────┤   │        │
        │ │ id                              │   │        │
        │ │ cross_reply_id          ├───────┴───┤        │
        │ │ agent_id        ├───────────────→ Agents     │
        │ │ conversation_id ├──────────────→ Conversations
        │ │ response_message_id ├──────────→ Messages    │
        │ │ created_at              │   │        │
        │ └─────────────────────────────────┤   │        │
        └───────────────────────────────────┘   │        │
                                                │        │
                                       ┌────────┘        │
                                       │                 │
                                    Agents ◄─────────────┘
                                    Conversations
                                    Messages
```

---

## API Call Sequence Diagram

```
┌────────┐                                    ┌─────────┐
│ Client │                                    │ Backend │
└────┬───┘                                    └────┬────┘
     │                                             │
     │ 1. POST /cross-replies                      │
     │    (Save original response)                 │
     ├────────────────────────────────────────────→│
     │                                             │
     │                200 Created                  │
     │         { id: "session-xyz" }              │
     │←────────────────────────────────────────────┤
     │                                             │
     │ 2. User chats with other bots...           │
     │                                             │
     │ 3. POST /cross-replies/:id/responses        │
     │    (Add Bot B's response)                   │
     ├────────────────────────────────────────────→│
     │                                             │
     │                200 Created                  │
     │←────────────────────────────────────────────┤
     │                                             │
     │ 4. POST /cross-replies/:id/responses        │
     │    (Add Bot C's response)                   │
     ├────────────────────────────────────────────→│
     │                                             │
     │                200 Created                  │
     │←────────────────────────────────────────────┤
     │                                             │
     │ 5. GET /cross-replies/:id                   │
     │    (Fetch all responses)                    │
     ├────────────────────────────────────────────→│
     │                                             │
     │               200 OK                        │
     │    { responses: [...all bot answers...] }  │
     │←────────────────────────────────────────────┤
     │                                             │
```

---

## State Management Flow

```
┌─────────────────────────────────────────────────────┐
│              React Component State                   │
└─────────────────────────────────────────────────────┘

  const [crossReplies, setCrossReplies] = useState([])
  const [activeSession, setActiveSession] = useState(null)
  const [loading, setLoading] = useState(false)

         │
         │ user clicks "Save for comparison"
         ▼
  1. createCrossReply()
     ├─ POST /api/agents/cross-replies
     └─ setActiveSession(newSession)

         │
         │ user chats with more bots
         ▼
  2. chatStream() [existing feature]
     └─ save last message ID

         │
         │ stream completes
         ▼
  3. addAgentResponse()
     ├─ POST /api/agents/cross-replies/:id/responses
     └─ setActiveSession(updatedSession)

         │
         │ render comparison view
         ▼
  UI: Display all bot responses side-by-side
      ├─ Bot A's answer
      ├─ Bot B's answer
      ├─ Bot C's answer
      └─ ... more bots
```

---

## Component Architecture

```
┌──────────────────────────────────┐
│    ChatInterface Component       │
│  ┌────────────────────────────┐  │
│  │  Message Display           │  │
│  │  ┌──────────────────────┐  │  │
│  │  │ Bot A: [response]    │  │  │
│  │  │ [💚 Like]            │  │  │
│  │  │ [📊 Compare] ◄───────┼──┼──→ Opens CrossReplyModal
│  │  └──────────────────────┘  │  │
│  └────────────────────────────┘  │
└──────────────────────────────────┘
              │
              │
       ┌──────▼──────────────────────┐
       │  CrossReplyModal Component  │
       │  ┌───────────────────────┐  │
       │  │ Original Question     │  │
       │  │ Bot A's Response      │  │
       │  │                       │  │
       │  │ [Select Another Bot]  │──┼──→ Opens BotSelector
       │  │ [Show Comparison]     │  │
       │  └───────────────────────┘  │
       └──────────────────────────────┘
              │
              │
       ┌──────▼──────────────────────┐
       │  ComparisonView Component   │
       │  ┌───────────────────────┐  │
       │  │ Q: [Original Ques]    │  │
       │  ├───────────────────────┤  │
       │  │ Bot A | Bot B | Bot C │  │
       │  ├───────────────────────┤  │
       │  │Response A              │  │
       │  │Response B              │  │
       │  │Response C              │  │
       │  │[⭐ Rate] [📋 Copy]   │  │
       │  └───────────────────────┘  │
       └──────────────────────────────┘
```

---

## HTTP Status Flow

```
Request: POST /api/agents/cross-replies
  ├─ 201 Created → Session successfully created ✅
  ├─ 400 Bad Request → Missing required fields ❌
  ├─ 401 Unauthorized → User not authenticated ❌
  └─ 500 Server Error → Database error ❌

Request: GET /api/agents/cross-replies/:id
  ├─ 200 OK → Session retrieved ✅
  ├─ 404 Not Found → Session doesn't exist ❌
  ├─ 401 Unauthorized → User not authenticated ❌
  └─ 500 Server Error → Database error ❌

Request: POST /api/agents/cross-replies/:id/responses
  ├─ 201 Created → Response added ✅
  ├─ 400 Bad Request → Missing fields ❌
  ├─ 404 Not Found → Session doesn't exist ❌
  ├─ 401 Unauthorized → User not authenticated ❌
  └─ 500 Server Error → Database error ❌

Request: DELETE /api/agents/cross-replies/:id
  ├─ 200 OK → Session deleted ✅
  ├─ 404 Not Found → Session doesn't exist ❌
  ├─ 401 Unauthorized → User not authenticated ❌
  └─ 500 Server Error → Database error ❌
```

This visual guide shows how all components interact to enable bot-to-bot conversations! 🎉
